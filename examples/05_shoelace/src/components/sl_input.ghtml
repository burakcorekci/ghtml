@import(gleam/dynamic/decode)

@params(
  value: String,
  label: String,
  placeholder: String,
  on_input: decode.Decoder(msg),
)

<sl-input
  value={value}
  label={label}
  placeholder={placeholder}
  @sl-input={on_input}
/>
